const express = require('express');
const router = express.Router();

//here i serve the GET request by sending the ejs file
//we load the logs of the chatroom, nothing else


module.exports = router;